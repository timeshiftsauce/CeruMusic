# Vue 计数页面

这是“开发 Vue 插件页面”教程的完成版。

1. 执行 `npm install`。
2. 执行 `npm run dev`，打开 `counter · web`。
3. 点击“加一”，关闭页面后重新打开，确认计数保留。
4. 执行 `npm run typecheck`、`npm run build` 和 `npm run validate`。

页面关闭会停止后台计时器，插件停用时也会执行清理函数。
